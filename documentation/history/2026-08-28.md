# 2026-08-28: Catalog alignment + declarative settings port (fleet review)

## Accomplished

Two commits from the fleet-review pass (obsidian-plugin-template is the canonical source):

1. **Catalog reviewer alignment (A-E)**: export-ignores dropped; `__PLUGIN_CHANGELOG__` build define replaces the markdown import; eslint-plugin-obsidianmd 0.1.9 → 0.4.1 with `ui/sentence-case` as an error (Ollama/Whisper brands, anchored exemptions for the quoted command name and the URL placeholder) and the flagged `createEl` div/span calls auto-fixed to `createDiv`/`createSpan`; type roots pinned + obsidian typings 1.13.1 (two `override` modifiers); `bun audit` clean — 33 advisories including the critical handlebars JS injection cleared (lodash 4.18.1 / tmp 0.2.7 overrides; the repo had none); template toolchain carrying the fleet's sourcemap-`'none'` and versions.json latest-floor fixes.
2. **Declarative settings port (feat!, minAppVersion 1.8.7 → 1.13.0)**: `getSettingDefinitions()` replaces the 311-line `display()` tab. Dynamic UI (model dropdown options, recommended-model rows) is recomputed per render; the installed-model refresh is loop-safe (re-renders only on actual change); the stored model stays visible as "(not found)" when not installed. New serialized persist-then-commit `updateSettings` replaces the optimistic inline writes in the tab AND the two model commands; the Ollama service re-reads its config strictly post-commit. Bug fixed along the way: `frontmatterTags` was missing from the settings-load backfill, so the saved value silently reset on every restart.

## Verification

94 tests (7 new write-path tests with mutation-checked persist-then-commit ordering and serialization; 4 latestMinAppVersion tests), tsc clean, `lint --max-warnings 0` clean, build green, archive simulation green.
